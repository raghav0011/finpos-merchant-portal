import React from 'react';

// Import custom components
import InternalServer from '../../exception/components/InternalServer';

const InternalServerContainer = () => {
  return <InternalServer />;
};

export default InternalServerContainer;
