import React from 'react';

// Import custom components
import NotFound from '../../exception/components/NotFound';

const NotFoundContainer = () => {
  return <NotFound />;
};

export default NotFoundContainer;
